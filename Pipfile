[[source]]
name = "pypi"
url = "https://pypi.org/simple"
verify_ssl = true

[dev-packages]
# Used for releasing package to pypi.
twine = "*"
bump2version = ">=1.0.0"
vcrpy = "*"

[packages]
parsel = ">=1.2.0"
lxml = ">=4.1.1"
cssselect = ">=1.0.3"
aiohttp = ">=3.0.9"

[requires]
python_version = "3"

[pipenv]
allow_prereleases = true
